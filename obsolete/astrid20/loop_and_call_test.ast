// Test for loop with variables and function calls
void main(){
    int8 colors[5];
    int16 i = 0;
    
    for(i = 0; i < 5; i++){
        colors[i] = i + 10;
    }
    
    draw_single_pixel(100, 50, 15);
    halt();
}

void draw_single_pixel(int8 x, int8 y, int8 color){
    set_layer(1);
    set_pixel(x, y, color);
}
