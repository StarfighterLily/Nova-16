// Test struct support
struct Position {
    int16 x;
    int16 y;
    int8 layer;
}

void main() {
    Position player;
    player.x = 100;
    player.y = 50;
    player.layer = 1;
    
    set_pixel(player.x, player.y, 15);
    halt();
}
