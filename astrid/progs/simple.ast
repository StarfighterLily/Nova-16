void main() {
    set_mode(1);
    set_layer(0);

    for (char x = 0; x < 128; x++) {
        for (char y = 0; y < 128; y++) {
            set_pos(x, y);
            write_screen(0x1F);
        }
    }
}
