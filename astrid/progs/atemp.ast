include "includes\debug.ast"

int fired;

interrupt(2) void kbd_int() {
    fired = fired + 1;
    int key = key_read();
    if (key) {
        dbg.print((string)key);
    }
    iret();
}

void main() {
    sti();
    key_ctrl(1);
    while (1) {
        
    }
}
