// Ultra-simple struct test
// Just draw pixels in a line to verify struct updates work

STRUCT Point
    x
    y
END

SetLayer(1)
ClrDraw

// Draw a horizontal line using struct
Point.y = 128

For i = 0 To 100
    Point.x = i
    PxlOn(Point.x, Point.y, 31)
Next

Disp "Line drawn!"
Pause
