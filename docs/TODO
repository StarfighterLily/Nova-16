-Layers
Dedicated text/UI layers? I mean sure the screen, BG, and sprite layers can be used, but dedicated layers can offer specific features
Create better font

-Registers
Bring back R register pairing:
  - R0:R1, R2:R3, R4:R5, R6:R7, R8:R9
  - High:Low bytes clearly delineated by ':' as with P register byte access

-MMX
Consider MMX-style registers and instructions

-Memory
Consider memory banking techniques to expand beyond the 64k memory without too much effort

-Mouse
4x4 sprite, position in 2 16-bit registers, shouldn't need anything more but the hardware. 2-button mouse to start with, then move up to many buttons and 3 wheels (hey, you never know.)
Cursor sprite:
1110
1100
1010
0001